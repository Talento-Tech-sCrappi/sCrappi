spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/db_scrappi #Aqui falta normalizar la BD
    # definir DBA y control de user
    username: postgres
    password: 123456

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8081
  error:
    include-message: always
